export interface RulesContent {
  markdown: string;
  label: string;
  fileName: `${string}`;
}

export interface SettingsContent {
  markdown: string;
  label: string;
  fileName: `${string}`;
}
